load('myarray.js'); 
var myarray = JSON.parse(MYARRAY_File);

function reduce(myarray, combine, start) {
  var current = start;
  for (var i = o; i < roster.length; i++)
    current = combin(current, myarray[i]);
    return current;
  }

print(myarray.reduce(function(highest, cur){
  highest = 1;      // setting current highest to 1 for base 
  if (myarray.randomnumber > highest) return cur; // if the random number on myarray is bigger it returns that
  else return highest;        // else it returns the current highest 
}))
