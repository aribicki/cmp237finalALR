load('myarray.js'); 
var myarray = JSON.parse(MYARRAY_File);

function reduce(myarray, combine, start) {
  var current = start;
  for (var i = o; i < roster.length; i++)
    current = combin(current, myarray[i]);
    return current;
  }

print(myarray.reduce(function(highest, cur)))
